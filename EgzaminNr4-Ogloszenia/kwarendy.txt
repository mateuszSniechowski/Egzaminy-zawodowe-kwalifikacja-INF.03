kw1: 
wybierające jedynie pola id, tytul i tresc z tabeli ogloszenie dla tych rekordów, dla
których kategoria to książki 

SELECT id, tytul, tresc FROM ogłoszenia where kategoria = 1



kw2:
wykorzystujące relację pomiędzy tabelami i wybierające jedynie telefon tego
użytkownika, który jest przypisany do ogłoszenia numer 1

SELECT uzytkownik.telefon
FROM uzytkownik
JOIN ogloszenia ON uzytkownik.id = ogloszenia.uzytkownik_id
WHERE ogloszenia.id = 1;

//w telefonie fota inny sposob ??? 



kw3:
tworzące użytkownika moderator na localhost z hasłem qwerty
CREATE USER 'moderator'@'localhost' IDENTIFIED BY 'qwerty';



kw4: ?????
nadające prawa dla użytkownika moderator do usuwania i przeglądania danych
w tabeli ogloszenie.
GRANT SELECT, DELETE ON ogloszenia TO 'moderator'@'localhost';









